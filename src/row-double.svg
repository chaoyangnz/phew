<svg width="{{width}}" height="{{height}}" font-family="Arial, Helvetica, sans-serif">
    <style>
        .primary {
            font-size: 5em;
            font-weight: bold;
            fill: {{font.color}};
            text-anchor: left;
        }

        .secondary {
            font-size: 4em;
            fill: #666;
            text-anchor: left;
        }

        .logo {
            width: 180;
            height: 180;
        }

        .separator {
            stroke: #555555;
            stroke-width: 2;
        }

        .left {
            transform: translate(100, 0);
        }

        .right {
            transform: translate({{add width -1100}}, 0);
        }
    </style>

    <g class="left">
        <text class="primary" x="0" y="35%">
            {{len.model}}
        </text>
        <text class="secondary" x="0" y="65%">
            {{camera.model}}
        </text>
    </g>

    <g class="right">
        <image class="logo" href="{{camera.logo}}" x="0" y="25%" />
        <line class="separator" x1="240" x2="240" y1="25%" y2="75%" />
        <text class="primary" x="300" y="35%">
            {{exposure.focal}}mm 𝓕{{exposure.aperture}} {{exposure.shutter}}s ISO{{exposure.iso}}
        </text>
        <text class="secondary" x="300" y="65%">
            {{datetime}}
        </text>
    </g>
</svg>
