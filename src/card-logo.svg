<svg width="{{width}}" height="{{height}}" font-family="Arial, Helvetica, sans-serif">
    <style>
        .logo {
            width: 180;
            height: 180;
            transform: translate({{divideAdd width 2 -90}}, {{divideAdd height 2 -90}});
        }
    </style>

    <image class="logo" href="{{camera.logo}}" x="0" y="0" />

</svg>
